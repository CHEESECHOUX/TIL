/*
public class ExceptionHandling {
  public void myMethod () {

    try {
      // 시도해 볼 동작
    } catch (Exception e) {
      // 오류 발생 시
    }
    // ... 이후 동작
  }
}


//
goTo(superMarket);

try {
  superMarket.buy("eggs");
} catch (NoMoneyException e) {
  // 계란 값이 가진 돈을 초과할 경우 동작
} catch (HalMoneyException e) {
  // 가게 주인 할머니가 안 계실 경우 동작
} finally {
  // 계란을 사든 못 사든 동작
}
comdBackHome();


//
public boolean lookForCarKey () {
  // ... (생략)
  deskDrawer.open(); // 책상 서랍을 연다
  try {
    deskDrawer.searchCarKey();
    return true;
  } catch (Exception e) {
    // 어디 있더라
  } finally {
    deskDrawer.close(); // 책상 서랍을 닫는다
  }
  // ... (다른 곳에서 찾기)
}
*/